import { BusquedaEstudiantePipe } from './busqueda-estudiante.pipe';

describe('BusquedaEstudiantePipe', () => {
  it('create an instance', () => {
    const pipe = new BusquedaEstudiantePipe();
    expect(pipe).toBeTruthy();
  });
});
