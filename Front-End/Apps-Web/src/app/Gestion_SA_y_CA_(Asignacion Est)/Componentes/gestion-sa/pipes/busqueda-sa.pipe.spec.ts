import { BusquedaSaPipe } from './busqueda-sa.pipe';

describe('BusquedaSaPipe', () => {
  it('create an instance', () => {
    const pipe = new BusquedaSaPipe();
    expect(pipe).toBeTruthy();
  });
});
