import { BusquedaCaPipe } from './busqueda-ca.pipe';

describe('BusquedaCaPipe', () => {
  it('create an instance', () => {
    const pipe = new BusquedaCaPipe();
    expect(pipe).toBeTruthy();
  });
});
